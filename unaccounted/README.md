# Unaccounted artifacts

This directory preserves project files that do not currently have an established
place in the application structure. Nothing placed here was deleted during the
layout reorganization.

Current contents include IDE metadata, a development metadata file, and the
legacy `flowChart.svg` architecture diagram.
